import React, { memo } from 'react';

import styles from './styles.module.scss';

const CardPreview = memo(() => {
    return <div className={styles.wrapper}>CardPreview</div>;
});

export default CardPreview;
